importance: 1

---

# Funcția min(a, b)

Scrieți o funcție `min(a,b)` care returnează cel mai mic dintre două numere `a` și `b`.

De exemplu:

```js
min(2, 5) == 2
min(3, -1) == -1
min(1, 1) == 1
```

